name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run unit tests
        run: python -m unittest discover -s tests -v

      - name: Run Ruff
        run: python -m ruff check .

      - name: Run Mypy (critical paths)
        run: |
          python -m mypy --follow-imports=skip \
            core/intelligence \
            core/artifacts \
            core/reporting \
            core/analyze \
            core/adapters \
            core/runner.py

      - name: Run compile smoke
        run: python -m compileall -q core filters modules plugins tests silica-x.py
